import 'package:flutter/material.dart';

class EighthScreen extends StatelessWidget {
  const EighthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Eighth Screen"),),);
  }
}
